PACKAGES=(
 
    firefox
    brave

    # ── NETWORKING / FIREWALL ─────────────────────────────────────────────
    ufw                          # firewall frontend
    nftables                     # backend (linux-hardened already loads nf modules)
    wireguard-tools
    nmap
    tcpdump
    wireshark-qt
    traceroute
    bind                         # dig
    net-tools                    # ifconfig, netstat
    iperf3
    openssl
    openssh


    # ── SECURITY ─────────────────────────────────────────────────────────
    firejail
    apparmor
    audit
     clamav
    rkhunter
    usbguard
 
    # ── DEV TOOLS ─────────────────────────────────────────────────────────
    git
     python
    python-pip
    python-virtualenv
    nodejs
    npm
    composer                     # PHP dependency manager
    docker
    docker-compose
    podman

    jq
    yq
    sqlite

    curl
    wget
    rsync
    strace
    ltrace
    gdb
    valgrind
    clang
    cmake
    make

    # ── SHELL & TERMINAL UTILS ────────────────────────────────────────────
    zsh
    zsh-completions
    zsh-autosuggestions
    zsh-syntax-highlighting
    starship                     # prompt
    tmux
    fzf
    ripgrep
    fd
    bat
    eza                          # modern ls
    duf                          # modern df
    dust                         # modern du
    htop
    btop
    fastfetch
    unzip
    zip
    p7zip
    tree

    # ── MULTIMEDIA (minimal — no full suite) ──────────────────────────────
    pipewire
    pipewire-alsa
    pipewire-pulse
    pipewire-jack
    wireplumber
    pavucontrol
    mpv
)

AUR=(
     
  
)